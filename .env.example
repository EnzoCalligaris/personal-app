# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Prisma (Postgres via Supabase)
# Connection pooling / pgbouncer, porta 6543 (usada em runtime pela aplicação)
DATABASE_URL=
# Conexão direta, porta 5432 (usada pelo Prisma CLI: migrate, generate, studio)
DIRECT_URL=
